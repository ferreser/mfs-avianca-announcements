// client.js (panel Web UI) -- ya incluido en README antes.
// Implementa bind -> POST /bind, WS register, commands and trigger sending.
// (Utiliza localStorage para token).
// Copia la versión completa del client.js que te proporcioné antes.